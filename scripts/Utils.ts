/**
 * Utils.ts — Standalone utility script. Not needed to run directly.
 * All utility functions are duplicated into each script file since
 * Office Scripts requires each script to be self-contained.
 *
 * This file is kept as a reference / single source of truth.
 * Entry point: main() does nothing (utility-only).
 */

function main(workbook: ExcelScript.Workbook): void {
  // Utility-only script. No action on direct run.
  console.log("Utils.ts loaded. This script contains shared utilities only.");
}
